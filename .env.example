# Safe default used by every automated and repeated verification path.
MODEL_MODE=fake
OPENAI_MODEL=gpt-5.6-luna
MODEL_REQUEST_TIMEOUT_MS=10000

# Never place a credential in this tracked file. The real-model override reads
# the ignored local file .secrets/openai_api_key.
